export default {
  id: ({ id }) => id,
  name: ({ name }) => name,
  rating: ({ rating }) => rating,
  address: ({ address }) => address,
  open: ({ open }) => open,
  photoReference: ({ photoReference }) => photoReference,
  coord: ({ coord }) => coord,
};
