export default {
  id: ({ id }) => id,
  email: ({ email }) => email,
  name: ({ name }) => name,
  createdAt: ({ createdAt }) => createdAt,
  deletedAt: ({ deletedAt }) => deletedAt,
  updatedAt: ({ updatedAt }) => updatedAt,
};
